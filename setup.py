from setuptools import setup


setup(
        name='abeona',
        version=0.1,
        keywords='Abeona',
        install_requires=[
            'pykep=2.5.1',
            'pygmo>=2.15',
        ],
)
